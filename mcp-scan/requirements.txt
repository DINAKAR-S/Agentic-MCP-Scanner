# Core: required for detection, scoring and reporting. No network, no API key.
cvss==3.6

# Optional: ingestion from a GitHub URL (a local path needs nothing).
gitingest==0.1.4

# Optional: --narrative
google-generativeai==0.8.5

# Optional: --threat-intel
firecrawl-py==2.16.3

# Convenience
python-dotenv==1.1.1
